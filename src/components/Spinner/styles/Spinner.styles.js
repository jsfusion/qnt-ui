import styled from 'styled-components';
import { Spinner as BPSpinner } from '@blueprintjs/core';
// import { colors, spacings } from '../../../settings';

export const StyledSpinner = styled(BPSpinner)`

`;
