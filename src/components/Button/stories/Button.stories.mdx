import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import StoryTag from '../../../internal/StoryTag';
import Button from '..';

<Meta title="Components/Button" />

<Story name="Import" />

# Button

Buttons are used to make common actions immediately visible and easy to perform with one click, tap, or keypress.

## Import

```jsx
import { Button } from '@qnt/ui';
```

<Story name="Props" />

## Props

<Props of={Button} />

<Story name="Usage" />

## Usage

### Primary

<Preview withToolbar>
  <Story name="1. Primary">
    <>
      <section>
        <Button variant="primary" size="small">Small Button</Button>
        <Button variant="primary">Medium Button</Button>
        <Button variant="primary" size="large">Large Button</Button>
      </section>
    </>
  </Story>
</Preview>

### Secondary

<Preview withToolbar>
  <Story name="2. Secondary">
    <>
      <section>
        <Button variant="secondary" size="small">Small Button</Button>
        <Button variant="secondary">Medium Button</Button>
        <Button variant="secondary" size="large">Large Button</Button>
      </section>
    </>
  </Story>
</Preview>

### Disabled

<Preview withToolbar>
  <Story name="3. Disabled">
    <>
      <section>
        <Button variant="primary" isDisabled>Primary Button</Button>
        <Button variant="secondary" isDisabled>Secondary Button</Button>
      </section>
    </>
  </Story>
</Preview>

### Loading

<Preview withToolbar>
  <Story name="4. Loading">
    <>
      <section>
        <Button variant="primary" isLoading>Primary Button</Button>
        <Button variant="secondary" isLoading>Secondary Button</Button>
      </section>
    </>
  </Story>
</Preview>

### Left Icon

<Preview withToolbar>
  <Story name="5. Left Icon">
    <>
      <section>
        <Button variant="primary" iconLeft="add">Primary Button</Button>
        <Button variant="secondary" iconLeft="add">Secondary Button</Button>
        <Button variant="primary" iconLeft="add" isDisabled>Primary Button</Button>
        <Button variant="secondary" iconLeft="add" isDisabled>Secondary Button</Button>
      </section>
    </>
  </Story>
</Preview>

### Right Icon

<Preview withToolbar>
  <Story name="6. Right Icon">
    <>
      <section>
        <Button variant="primary" iconRight="add">Primary Button</Button>
        <Button variant="secondary" iconRight="add">Secondary Button</Button>
        <Button variant="primary" iconRight="add" isDisabled>Primary Button</Button>
        <Button variant="secondary" iconRight="add" isDisabled>Secondary Button</Button>
      </section>
    </>
  </Story>
</Preview>

### Callback

<Preview withToolbar>
  <Story name="7. Callback">
    <>
      <section>
        <Button variant="primary" onClick={() => alert('Primary Button Clicked!')}>Primary Button</Button>
        <Button variant="secondary" onClick={() => alert('Secondary Button Clicked!')}>Secondary Button</Button>
      </section>
    </>
  </Story>
</Preview>
