import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import Icon from '../../src/components/Icon';
import { colors } from '../../src/settings';

export const GreenTick = () => <Icon icon="tick" size={20} color={colors.rag.green.ColorGreenDark}/>;

export const RedCross = () => <Icon icon="cross" size={20} color={colors.rag.red.ColorRedDark}/>;

<Meta title="Get started/Component Status" />

# Component Status

> <i style={{color: 'red'}}>We are busy in developing various components for you, we'll come up with a first release of the library very soon...</i>

| Component | Active | Deprecated |
| --------- | ----------- | ------ |
| `Button` | <GreenTick /> | <RedCross /> |
| `Heading` | <GreenTick /> | <RedCross /> |
| `Highlight` | <GreenTick /> | <RedCross /> |
| `Icon` | <GreenTick /> | <RedCross /> |
| `Label` | <GreenTick /> | <RedCross /> |
| `Paragraph` | <GreenTick /> | <RedCross /> |
